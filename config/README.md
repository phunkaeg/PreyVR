# Configuration

Commit portable defaults and documented schema changes here. Keep headset-specific calibration, local paths, and developer overrides under `config/local/` (ignored).

Every setting that changes an engine integration must identify the build/module it applies to and have a safe default that preserves desktop rendering.
