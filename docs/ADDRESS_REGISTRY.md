# Address registry

Only confirmed or explicitly provisional build-specific locations belong here. Prefer a signature plus validation recipe over an RVA alone.

| ID | Module | RVA | Symbol / purpose | Signature | Evidence | Confidence | Validation / status |
| --- | --- | ---: | --- | --- | --- | --- | --- |
| R-001 | `PreyDll.dll` | `0x1D93408` | D3D dynamic-loader string cluster | ASCII `CreateDXGIFactory1\0` followed by `dxgi.dll`, `D3D11CreateDevice`, `d3d11.dll` | Ghidra memory inspection 2026-07-30; xrefs resolved after the full analysis pass, 2026-08-01 | observed | Consumers identified: `0x180F50057` in R-025 and `0x180D87761` in R-046. **Both are now classified as of 2026-08-22** — R-025 is the real device/swapchain creation path and R-046 is machine-spec detection telemetry that creates and immediately releases throwaway devices. There is no second render or present path behind these strings. Anchor only; still do **not** hook or patch the strings themselves. |
| R-025 | `PreyDll.dll` | `0xF50000` | `InitializeD3D11DeviceAndSwapChain`; D3D11 device / adapter / swapchain creation | `48 89 5C 24 18 88 54 24 10 55 56 57 41 54 41 55 41 56 41 57 48 8D 6C 24 F9 48 81 EC B0 00 00 00` | Ghidra decompilation after the 2026-08-01 analysis pass; signature uniqueness and on-disk match verified | static-only | Loads `dxgi.dll`/`d3d11.dll` dynamically, creates the factory with the IID at `0x181D933F8`, selects an adapter in an `EnumAdapters1` loop (factory vtable `+0x60`) terminating on `DXGI_ERROR_NOT_FOUND`, then creates the device through one of two vendor-extension paths (`FUN_180F621A0` / `FUN_180F623B0`, selected by object `[0x58]`) or plain `D3D11CreateDevice` at feature level `0xB000`. Swapchain via factory `+0x50`, then `MakeWindowAssociation(hwnd, 3)` via `+0x40`. **Adapter selection resolved 2026-08-22, and it is directly actionable for `XR_KHR_D3D11_enable`.** R-025 opens by calling R-052 and then executes `MOVSXD RDI,EAX; MOV R13D,R14D; CMOVNS R13D,EDI; MOV EDX,R13D; CALL [R9+0x60]` — so a **non-negative `r_overrideDXGIAdapter` becomes the adapter index handed to `EnumAdapters1` for the game's real D3D11 device**, and a negative value falls back to scanning from index 0. OpenXR requires the app's device to be created on the adapter LUID that `xrGetD3D11GraphicsRequirementsKHR` returns, which on a multi-GPU or hybrid-graphics machine need not be the one Prey would pick. This cvar is a **native, supported lever for that, requiring no hook at all**. Two constraints travel with it: it is an adapter *index*, not a LUID, so the mod must enumerate adapters itself to translate the required LUID into an index; and it is read once during device creation, so it has to be set before the renderer initialises rather than adjusted later. Object layout is in the device-creation capture. The 32-byte prologue is unique in the image and is now the 22nd fail-closed landmark; the build doctor matches it in the installed Steam DLL. Confidence stays `static-only` because no live entry has been captured — it has never been observed executing, and its object layout is inferred from COM slot usage rather than a runtime read. Do **not** hook it until that exists. |
| R-002 | `PreyDll.dll` | `0xF7D710` | `CD3D9Renderer::RT_BeginFrame` (was `BeginRendererScene`) | `48 8B C4 55 53 48 8D 68 A1 48 81 EC B8 00 00 00 4C 89 78 E8 48 8B D9` | Ghidra decompilation plus live vtable resolution at slot `0x8C8`, 2026-07-31 | observed | Paired predecessor of R-003. Headless signature check is in the build doctor. Semantics are strong but not yet mutation-tested. **Name corrected 2026-08-15** by reverse translation to EGS `0xF51650`, `FRT_BeginFrame` at `DriverD3D.h:1049`. Its prologue matches **twice** in EGS (`0xF51650` and `0x141CAD0`); the correct one was selected by spacing, since RT_EndFrame sits `0xB00` after RT_BeginFrame in both builds. The rejected candidate is `ArkCystoid::ProcessNearbyCystoids`. A generic MSVC prologue with no member offsets is a weak cross-build anchor — prefer an interior anchor if re-derived. **Signature extended 2026-08-22 from 16 to 23 bytes.** The 16-byte form also occurs twice in the *Steam* image, at `0xF7D710` and `0x1449620`; the two diverge at byte 16, where this one continues `4C 89 78 E8` and the collision has `48 8B 59 38`. The gate was never wrong — it compares bytes at a fixed RVA rather than scanning — but the short form could not identify the function on its own. 23 bytes is instruction-aligned and unique. |
| R-003 | `PreyDll.dll` | `0xF7E210` | `CD3D9Renderer::RT_EndFrame` (was `EndRendererScene`); primary engine frame boundary | `40 57 48 83 EC 60 83 B9 F8 AE 00 00 00 48 8B F9` | Game diagnostic xref, live vtable/CE cadence capture, Ghidra ABI confirmation, and bounded PreyVR observer, 2026-07-31 to 2026-08-01 | reproduced | The default-off trampoline observed 301 calls with stable renderer `0x7FFE20544E80` and thread `780692`, then disabled and restored this exact prologue before DLL unload. Callback mutation remained none. See the frame-observer capture. **Name corrected 2026-08-15** by reverse translation to EGS `0xF52150`, `FRT_EndFrame` at `DriverD3D.h:1050`, unique match. The PDB signature `void(CD3D9Renderer* _this)` independently confirms the ABI the frame observer had assumed from decompilation alone. |
| R-004 | `PreyDll.dll` | `0xFE9D14` | Render-command dispatch to `EndRendererScene` | `4C 8B 15 C5 4B B5 01 49 8B CA 49 8B 02 FF 90 D0 08 00 00` | Ghidra disassembly and live CE register capture, 2026-07-31 | reproduced | Loads the renderer singleton from `PreyDll.dll+0x2B3E8E0`, then calls vtable slot `0x8D0`. The call instruction is at RVA `0xFE9D21`. |
| R-005 | `PreyDll.dll` | `0x2B3E8E0` | Pointer to the `CD3D9Renderer` singleton | RIP-relative reads from R-004 and related renderer paths | Ghidra xrefs, CE qword read, and ReGenny object inspection, 2026-07-31 | observed | Current session value pointed to a module-resident object whose vtable slot `0x8D0` resolves to R-003. **Class identified 2026-08-15 as `CD3D9Renderer`** (`RenderDll/XRenderD3D9/DriverD3D.h`, `class CD3D9Renderer : public CRenderer`), no longer provisional. RTTI is still absent in the binary; the identification comes from matching the PDB-derived member chain to three independently measured offsets — `m_pSwapChain` at `+0xAE88`, `m_SceneRecurseCount` at `+0xAEF8`, and `m_DeviceWrapper` at `+0xAF28` — with the members between them closing byte-exactly and no slack. The EGS and Steam layouts therefore agree across this region. |
| R-006 | R-005 renderer object | `+0xAE88` | `IDXGISwapChain*` | R-003 at `PreyDll.dll+0xF7E48A`: `48 8B 8F 88 AE 00 00 ... 41 FF 51 40` | Static COM-slot identification, live pointer/vtable module inspection, and Present register equality, 2026-07-31 | reproduced | Vtable slot 8 is called with computed SyncInterval and flags. The loaded pointer exactly matched Present's live `RCX`. A mirror exists at `+0xAFA8`. Named 2026-08-15 as `CD3D9Renderer::m_pSwapChain`; it anchors the verified member chain described in R-005. |
| R-007 | R-005 renderer object | `+0xAF28` | `ID3D11Device*` | R-003 at `PreyDll.dll+0xF7E4DA`: `48 8B 8F 28 AF 00 00 48 8B 01 FF 90 38 01 00 00` | Static COM-slot identification, D3D11 vtable ownership, and DXGI failure control flow, 2026-07-31 | reproduced | Vtable slot 39 is `ID3D11Device::GetDeviceRemovedReason`. A mirror exists at `+0xAF98`. Named 2026-08-15 as `CD3D9Renderer::m_DeviceWrapper`, a `CCryDeviceWrapper` whose first member is the real `D3DDevice*` — which is why reading a qword here and calling slot 39 on it works. Acquire the immediate context through the SDK method rather than relying on adjacent provisional fields. |
| R-008 | `PreyDll.dll` | `0x157C990` | `GetArkPlayerInstance` | `48 83 EC 28 48 8B 0D 8D 10 CD 00 48 8B 01 FF 50 70` | Chairloader PDB bridge, matching Steam control flow, and live player-object correlation, 2026-07-31 | observed | Resolves the local player through the framework/actor system using player id `0x7777`. The nullable variant is at RVA `0x157C9C0`. |
| R-009 | `PreyDll.dll` | `0x148B820` | `ArkPlayerCamera::UpdateView(SViewParams&)` | `48 8B C4 55 53 56 57 41 55 41 56 41 57 48 8D A8 28 FF FF FF 48 81 EC A0 01 00 00` | PDB bridge plus live x64dbg entry capture, 2026-07-31 | reproduced | Live `RCX` was `ArkPlayer+0x12A0`; `RDX` held plausible `SViewParams` position/quaternion/FoV data. At RVA `0x148BB59`, a non-null callback at camera `+0x148` receives the same `SViewParams` and bypasses the default camera-mode path. **Disassembly re-verified 2026-08-22, and this is a native override the engine already honours** — the pattern a cross-engine peer flagged as worth finding *before* hooking a matrix write: `MOV RCX,[RDI+0x148]; TEST RCX,RCX; JZ default; MOV RAX,[RCX]; MOV RDX,RSI; CALL [RAX+0x10]; JMP end`. The engine tests the pointer **every frame**; when non-null it invokes the callable with `SViewParams&` in `RDX` and then **jumps past the entire default path**, which otherwise begins by reading the camera mode at `+0x1A4`. Prey's form is better shaped than a matrix-pointer override: it hands us the out-parameter to fill, so there is no race against the engine's own write. Its supported installer is R-010, already gated as `camera.set_custom_view`. |
| R-010 | `PreyDll.dll` | `0x1456460` | `ArkPlayerCamera::SetCustomViewFunction` | `48 89 5C 24 08 48 89 7C 24 10 55 48 8D 6C 24 A9 48 81 EC A0 00 00 00` | PDB bridge and paired decompilation, 2026-07-31 | static-only | Copies an MSVC `std::function<void(SViewParams&)>`; its callable manager/target is held at camera `+0x148`. The ArkPlayer forwarding wrapper is RVA `0x15CA580` and passes `ArkPlayer+0x12A0`. ABI-compatible installation and removal still need a bounded prototype. |
| R-011 | `PreyDll.dll` | `0x1585320` | `ArkPlayer::UpdateCachedReticleViewPosAndDir` | `40 55 57 48 8D AC 24 68 FF FF FF 48 81 EC 98 01 00 00 48 8B F9` | PDB bridge, live per-frame hit, decompilation, and reversible state probe, 2026-07-31 | reproduced | Unprojects the separate screen reticle into a world ray. With camera state frozen, changing only reticle X from `0.500` to `0.625` changed the normalized direction from about `(-0.947,-0.246,-0.209)` to `(-0.969,+0.156,-0.192)`. Original bytes were restored before resume. |
| R-012 | ArkPlayer object | `+0x12A0`, `+0x17D4`, `+0x17E0`, `+0x17EC` | Camera; cached reticle ray origin; cached reticle ray direction; reticle screen position | Field accesses in R-009/R-010/R-011 plus live pointer/value inspection | PDB bridge and x64dbg, 2026-07-31 | reproduced | `+0x12A0` is `ArkPlayerCamera`. The two `Vec3` ray fields and `Vec2` screen coordinate form the current detached-aim seam. The alternate reticle source at `+0xA10` is selected in one player state and remains to be classified. |
| R-013 | `PreyDll.dll` | `0x157CBB0` | `IArkPlayer::GetReticleViewPositionAndDir` | `8B 81 94 17 00 00 89 02 8B 81 98 17 00 00 89 42 04 ... 8B 81 A8 17 00 00 89 42 14 48 8B C2 C3` | Exact EGS/Steam function match plus immediate live hit, 2026-07-31 | reproduced | `RCX` is the `IArkPlayer` subobject at `ArkPlayer+0x40`; therefore interface offsets `+0x1794/+0x17A0` resolve to ArkPlayer `+0x17D4/+0x17E0`. Several native systems call this virtual getter. |
| R-014 | `PreyDll.dll` | `0x1694890` | `CArkWeapon::GetReticleInfoForFiring` | `48 89 74 24 18 55 57 41 56 48 8D 6C 24 B0 48 81 EC 50 01 00 00 45 33 F6` | PDB bridge and paired decompilation, 2026-07-31 | static-only | For local-player owner id `0x7777`, consumes R-013's ray, constructs a point along it, performs a physics query, and returns `ReticleInfo { IEntity*, Vec3 position }`. This is direct evidence that the detached ray reaches native weapon firing logic; a live impact test remains. |
| R-015 | `PreyDll.dll` | `0x1694A20` | `CArkWeapon::GetReticlePosition` | `48 89 5C 24 08 57 48 83 EC 40 33 C0 48 8B F9 48 83 C1 08 48 89 02 89 42 08` | PDB bridge and paired decompilation, 2026-07-31 | static-only | Returns `rayOrigin + rayDirection * weaponReticleRange` for the local player. `CArkWeapon::FindIronsightsTarget` at RVA `0x16930A0` consumes the same cached ray for target queries. |
| R-016 | `PreyDll.dll` | `0x159AF10` | `ArkPlayerMovementController::GetMovementState` | `40 53 48 83 EC 30 48 8B 0D 43 2B CB 00 48 8B DA 48 8B 01 FF 90 88 03 00 00` | PDB bridge and paired decompilation, 2026-07-31 | observed | Writes distinct view vectors at movement-state offsets `+0x54` and `+0x60`. Registered script callbacks `GetAimDir` (RVA `0x180AC60`) and `GetHeadDir` (RVA `0x180BA90`) retrieve different `SMovementState` vectors, corroborating native head/aim separation. **Traced 2026-08-22 for H-008.** It reads the **same** camera as R-011 — `ISystem::GetViewCamera()` through R-039 — and fills the caller's struct from that `Matrix34`: `+0x54` takes the translation column `{+0x0C, +0x1C, +0x2C}` (camera position) and `+0x60` takes column 1 `{+0x04, +0x14, +0x24}` (the Y axis, forward under CryEngine's forward=Y convention). Field *names* are unconfirmed; `SMovementState` is only forward-declared in the Chairloader headers. **Crucially it caches nothing** — every write lands in the caller-supplied `SMovementState&` (`+0x54`, `+0x5C`, `+0x60`, `+0x68`, `+0xB0`); nothing is written to a global or to the ArkPlayer object. It is a pure on-demand query, structurally unlike R-011 which caches into persistent ArkPlayer fields. A per-eye camera can therefore only affect a call made *while* an eye camera is set, and nothing survives the restore. **Caller enumeration was not possible**: `GetMovementState` is a pure virtual on `IMovementController`, so all calls dispatch through the vtable and no direct xref exists. The inherited claim that the script wrappers are not per-frame hooks was therefore not re-verified — the low-risk conclusion rests on the absence of caching, not on call frequency. |
| R-017 | `PreyDll.dll` | `0x16B1FD0` | `ArkWeaponWrench::OnActionAttackPrimary` | `48 89 5C 24 10 48 89 74 24 18 57 48 83 EC 30 41 83 F9 04 41 8B D9 48 8B F9 0F 94 C2 88 91 30 05 00 00` | PDB bridge and live x64dbg press-transition capture, 2026-07-31 | reproduced | Entry received the live wrench in `RCX`, local-player entity id `0x7777` in `EDX`, transition `1` in `R9D`, and input value `1.0` on the stack. This is the native primary-action lane, not yet a controller injection seam. |
| R-018 | `PreyDll.dll` | `0x16B2BC0` | `ArkWeaponWrench::OnHit` | `48 89 5C 24 10 48 89 74 24 18 57 48 83 EC 30 F3 0F 10 99 28 05 00 00 48 8B F9 F3 0F 10 81 24 05 00 00 0F 28 D3 F3 0F 5D C3 4C 8B C7 0F B6 87 31 05 00 00` | PDB bridge and live animation-impact capture, 2026-07-31 | reproduced | Hit once during the wrench swing with the same weapon object as R-017. `dir` and `damageScale` arrive in XMM registers and were not readable through the current debugger MCP. |
| R-019 | `PreyDll.dll` | `0x13BF730` | `ArkWrenchComponent::OnHit` | `48 8B C4 4C 89 40 18 55 41 54 41 56 48 8D A8 98 FD FF FF 48 81 EC 50 03 00 00 80 79 3F 00 4D 8B F0` | PDB bridge, caller mapping, and paired decompilation, 2026-07-31 | observed | Owns the deeper melee-hit flow and calls R-020. The exact prologue is promoted; a focused entry-register capture remains optional because R-020 supplied the decisive query evidence. |
| R-020 | `PreyDll.dll` | `0x13BD620` | `ArkWrenchComponent::GetHits` | `48 8B C4 48 89 50 10 48 89 48 08 55 53 56 57 41 55 41 56 41 57 48 8D A8 88 FC FF FF 48 81 EC 40 04 00 00` | PDB bridge, decompilation, and live x64dbg return-buffer/A0b captures, 2026-07-31 and 2026-08-01 | reproduced | ABI was `RCX=component`, `RDX=hidden std::vector<ray_hit> return`, `R8=weapon`, angle in `XMM3`; the component was weapon `+0x4C8`. The function obtains the cached reticle ray through R-013. A two-swing stack-local A0b changed only its copied direction by +10 degrees: the same wall collider remained at effectively identical depth while the contact moved `0.127165` units laterally. Both raw `0x50` records are deterministic fixtures. |
| R-021 | `PreyDll.dll` | `0x15850DC` | `ArkPlayer::Update` dispatch to `ArkPlayerInteraction::Update` | `48 8D 8F C8 0A 00 00 0F 28 CE E8 35 FD 00 00 48 8D 8F 98 18 00 00` | Exact EGS/Steam control-flow bridge, disassembly, and live return capture, 2026-07-31 and 2026-08-01 | reproduced | Forms `RCX=ArkPlayer+0xAC8`, then calls interaction update at `0x1594E20`. Live stops at return RVA `0x15850EB` confirmed `RDI=ArkPlayer` and the usable entity at ArkPlayer `+0xF34`: baseline keypad `0xFDE0`, then `0x1117` after only a transient -15-degree ray edit. |
| R-022 | `PreyDll.dll` | `0x159A660` | `ArkPlayerTargetSelector::UpdateCandidates` | `40 55 41 54 41 57 48 8D 6C 24 80 48 81 EC 80 01 00 00 48 8B 51 28` | Chairloader PDB header, unique Steam byte match, paired decompilation, and live changed-target A0 capture, 2026-07-31 and 2026-08-01 | reproduced | Clears the candidate vector at selector `+0x20`, obtains R-013's cached reticle ray, scales the direction by interaction distance at `+0x0C`, and runs native ray/broadphase/LOS selection. The live edge test wrote only `[RSP+0x5C..0x67]`; the exact edited direction propagated into the 2.5-unit native query, reduced two keypad records to one under +15 degrees, and selected entity `0x1117` instead of keypad `0xFDE0` under -15 degrees while the persistent ray remained unchanged. |
| R-023 | `PreyDll.dll` | `0x1593690` | `ArkPlayerInteraction::Interact` | `40 53 55 56 41 56 48 83 EC 68 48 8B E9 48 63 F2` | Chairloader PDB header, unique Steam byte match, and paired decompilation, 2026-07-31 | static-only | Reads usable entity id at interaction `+0x46C`, evaluates the per-mode `ArkInteractionInfo` at `+0x128 + mode*0x18`, and calls native `PerformInteraction` at `0x1593980`. This is the execution lane to preserve; detached motion aim should steer R-022 rather than simulate use actions. |
| R-024 | `PreyDll.dll` | `0x16914F0` | `CArkWeapon::AttachToHand` | Prologue `48 89 5C 24 08 48 89 6C 24 10 48 89 74 24 18 48 89 7C 24 20 41 56 48 81 EC 90 00 00 00`; unique interior begins at `+0x27` | Chairloader PDB header, unique Steam interior-byte match, and paired decompilation, 2026-07-31 | static-only | Steam decompilation confirms the equipped weapon consumes its resolved `IAttachment*` at `CArkWeapon+0x2B0`, installs a binding through attachment vtable slot `+0xD8`, and updates visibility/shadow-hand state. This is a visual-hand/weapon ownership lead, not yet a safe late-frame transform seam and therefore not promoted to the inert DLL gate. |

| R-026 | R-005 renderer object | `+0x4A08`, stride `0x328`, slot index at `+0x499C` | Per-frame render view block: camera transform, frustum, near/far | Located by scanning for an orthonormal 3x3 rotation followed by a plausible world position | Live read-only ReGenny probe, 2026-08-01 | reproduced | Two slots, indexed by `+0x499C`. **The block at `+0x240` is a `CRenderCamera`**, identified 2026-08-15 from Chairloader's PDB-derived `CryRenderer/IRenderer.h`. Its 72-byte layout matches every measured offset: `Vec3 vX, vY, vZ` at `+0x240` (the camera basis; `vY` is camera-up, which read near world Z only because the player was level), `Vec3 vOrigin` at `+0x264`, `float fWL, fWR, fWB, fWT` at `+0x270`, and `float fNear, fFar` at `+0x280`. A position copy sits at `+0x230`, aspect at `+0x040`, and a 4x4 rotation restatement at `+0x2E8`. Proven live: position moved `0.67244` units over 100 frames while walking and Z showed head bob. **Exactly one camera per slot**; the two slots are byte-identical when stationary, so they are frame double-buffers, not eyes. Frustum edges are four independent values, so asymmetric per-eye projection is directly representable. Never written to. |
| R-027 | R-005 renderer object | `+0xAC30` (2 entries), counter `+0xAC28` | Double-buffered GPU timing queries | Both entries share vtable `d3d11.dll` with 11 contiguous entries | Live read-only ReGenny probe, 2026-08-01 | reproduced | Recorded to close a refuted hypothesis. These were the leading `CRenderView` candidates from `EndRendererScene`'s decompilation; they are `ID3D11Query`-shaped system COM objects alternated by frame parity, not render views. `ID3D11DeviceContext` would show ~145 vtable entries. Do not re-investigate as a view structure. |

| R-028 | R-005 `CD3D9Renderer` | `+0xAEF8` | `m_SceneRecurseCount`; Begin/EndScene **recursion** depth | Loaded and tested by R-003's entry guard | Live ReGenny read plus PDB-derived member-chain arithmetic, 2026-08-15 | reproduced | Guards the `EndScene without BeginScene` diagnostic. The PDB name is a *recursion* count, not a boolean, so `BeginRendererScene`/`EndRendererScene` nest. Read `1` and `0` in the live probe depending on sampling point. **Tested for H-007 on 2026-08-15 and found not to detect it.** The value never exceeded `1` in any `e_ArkLookingGlass` mode at ~4,400 samples per frame, including while the second scene was demonstrably rendering full-screen. Prey's second scene is therefore produced without nesting `BeginRendererScene`/`EndRendererScene`, so this counter cannot observe it. The field itself is correctly identified and its 0/1 oscillation tracks the scene pass; only the inference drawn from it was wrong. |

| R-029 | `PreyDll.dll` | `0x243A688` | Pointer to the engine cvar object; `e_ArkLookingGlass` at `+0x5C4`, `e_ArkLookingGlassDebug` at `+0x5C8` | Registration in `FUN_180235400`: `LEA R8,[RBX+0x5C4]` / `MOV R9D,1` / `LEA RDX,[0x181C9C330]`, and `LEA R8,[RBX+0x5C8]` / `XOR R9D,R9D` / `LEA RDX,[0x181C9C3C8]` | Disassembled registration, decompiled consumer, and live read/write with restore, 2026-08-15 | reproduced | **Cvars are plain `int`s in a heap object, so Prey shipping without a developer console does not prevent setting them.** Both fields read their registered defaults live before any write, which independently confirms the object. `+0x5C8` also matches the decompiled consumer `*(int *)(DAT_18243a688 + 0x5c8)`. Writing `3` to `+0x5C4` rendered the second scene alone and full-screen; restored and verified. The object is heap-allocated, so its address is session-specific — resolve it through the module-relative slot every time, never cache it. |

| R-030 | `PreyDll.dll` | `0xEE7E80` | `CRenderView::SetCamera(const CCamera&)` | `48 8B C4 48 89 58 08 48 89 78 10 55 48 8D 68 D8 48 81 EC 20 01 00 00 0F 29 70 E8 48 8B F9 0F 29 78 D8 48 81 C1 A0 11 00 00 44 0F 29 40 C8 48 8B DA 44 0F 29 48 B8 44 0F 29 50 A8 44 0F 29 58 98` | Translated from Chairloader PDB header `RenderDll/Common/RenderView.h` (`FSetCamera` at EGS RVA `0xEBB960`) by unique byte match, 2026-08-15 | static-only | First function translated from the 14,622-entry PDB address table. The 64-byte prologue matches **exactly once** in the Steam image, and both function bodies are `0x349` bytes — EGS `0x180EBB960..0x180EBBCA9`, Steam `0x180EE7E80..0x180EE81C9`. The prologue includes `add rcx, 0x11A0`, a `CRenderView::m_camera` member offset, and it matched verbatim, so the two builds agree on that layout. **Never observed executing**; confidence stays `static-only` and it must not be hooked until a live entry is captured. **Promoted into the fail-closed gate 2026-08-22** as `view.set_camera`; the build doctor matches it in the installed DLL. **Fully characterised 2026-08-22** — 842 bytes, RVA `0xEE7E80`..`0xEE81CA`. It (1) copies the camera by value into `m_camera` at `+0x11A0` (R-049) via `CCamera::operator=`, so it cannot alias the system camera; (2) builds an orthonormal basis from the camera matrix and calls `CRenderCamera::LookAt` (R-051); and (3) computes the frustum tangents from `m_fov` `+0x30`, `m_ProjectionRatio` `+0x40` **and the asymmetry fields** as `fWL = m_asymL - t*ratio`, `fWR = t*ratio + m_asymR`, `fWB = m_asymB - t`, `fWT = t + m_asymT` where `t = tanf(m_fov*0.5)`, then stores near/far from `m_edge_nlt.y` `+0x4C` and `m_edge_flt.y` `+0x64` into the `CRenderCamera` block (R-050). It is **virtual**, at `CRenderView` vtable slot 8 (`+0x40`, R-053), so it can be taken by vtable swap instead of byte patching. **Observed live 2026-08-29**: 33 calls/second on thread 44208 from **exactly one** caller, `PreyDll+0x2110E5`, alternating between the two `[t][0]` pooled views. A single call site makes the deny-by-default return-RVA gate trivially implementable. **Its frustum formula was also corrected by that capture** -- see R-050. **This is the binary proof that Prey's asymmetric-frustum path is live in the render pipeline, not dead code** — and it makes this seam strictly better than writing the system camera: an OpenXR per-eye projection is expressed by setting four floats on the camera handed to it. |

| R-031 | `PreyDll.dll` | `0xEDF390` | `CRenderView::CRenderView(const char* name, EViewType type, CRenderView* pParentView, ShadowMapFrustum*)` | `48 89 5C 24 08 48 89 6C 24 10 48 89 74 24 18 57 41 54 41 55 41 56 41 57 48 83 EC 20 45 33 ED 44 89 41 14 44 89 69 10` | Translated from EGS `0xEB2DF0` (`FCRenderView`) by unique byte match, 2026-08-15 | static-only | Signature deliberately stops at 39 bytes, before the `LEA RAX,[RIP+...]` vtable load, because a RIP-relative displacement is build-specific and would never match. `44 89 41 14` stores the third argument (`EViewType`) to `this+0x14`, so **`CRenderView::m_viewType` is at `+0x14`**. Never observed executing.  **Promoted into the fail-closed gate 2026-08-22** as `view.ctor`; verified unique and byte-correct at its RVA in the installed DLL by the build doctor.|
| R-032 | `PreyDll.dll` | `0xFE5620` | `CRenderer::GetRenderViewForThread(int nThreadID, bool bRecursive)` | `48 63 D2 41 0F B6 C0 48 8D 04 50 48 8B 84 C1 38 6F 00 00 C3` | Translated from EGS `0xFB8FE0` by unique byte match, 2026-08-15 | static-only | A 20-byte leaf that computes `return m_pRenderViews[nThreadID*2 + bRecursive]`. **Formula confirmed by emulation on 2026-08-16**, not merely read from disassembly: with `RCX` pointing at the image base so the pool overlays known bytes, the four input combinations returned the qwords at `+0x00`, `+0x08`, `+0x10` and `+0x18` respectively. The `nThreadID=1, bRecursive=0` case returned slot **2**, which rules out the alternative ordering `nThreadID + bRecursive*2`. Ghidra had not auto-created a function here (20 bytes, leaf); it was created as `CRenderer_GetRenderViewForThread` with body size 20, matching the byte count exactly. **Recursive views are not constructed on demand** — `SRenderPipeline::m_pRenderViews[2][2]` is a permanent pool. The `+0x6F38` displacement matched verbatim across both builds, so they agree on the pool's offset. See R-033.  **Promoted into the fail-closed gate 2026-08-22** as `view.get_for_thread`; verified unique and byte-correct at its RVA in the installed DLL by the build doctor.|
| R-033 | R-005 `CD3D9Renderer` | `+0x6F38` | `SRenderPipeline::m_pRenderViews[2][2]`; four `CRenderView*` indexed `[nThreadID][bRecursive]` | Displacement inside R-032 | Static, from R-032's disassembly and `RenderDll/Common/RenderPipeline.h`, 2026-08-15 | reproduced | Element `[t][r]` sits at `+0x6F38 + (t*2 + r)*8` — **the indexing arithmetic is confirmed by emulating R-032** (see its entry), so only the base offset and the meaning of the first dimension remain untested. That first dimension is very likely the same MT/RT pair as the frame-slot index at `+0x499C`, which also alternates 0/1 — **plausible but unverified; do not rely on it**. **Not yet read live**: confirm all four entries are non-null heap pointers, and that `[t][1]` differs from `[t][0]`, before treating this as a usable seam. **That test is now implemented and shipped, 2026-08-27** — `PreyVR_CaptureRenderViews` runs exactly it, checks each entry's vtable against R-053 so a wrong pool offset is reported rather than decoded, and logs the verdict as `preyvr_renderview r033 all_four_non_null=… recursive_distinct=…`. Awaiting a live host. **ACCEPTANCE TEST PASSED LIVE 2026-08-29.** All four entries non-null, all four vtables matched R-053 at `0x7FFD1591AE00`, and `[t][1]` distinct from `[t][0]`. `[t][0]` hold the live camera; `[t][1]` are permanently allocated but never given a real camera (640x480, fov 0.9599, `fW -1,1,-1,1`, near 1.4142, far 10) -- idle, not in use. `SetCamera` was observed alternating between the two `[t][0]` views, which is the MT/RT double-buffer in motion. See [live capture](../captures/traces/2026-08-29-prey-hurdle1-live-capture.md). |

| R-034 | `PreyDll.dll` | `0xEE44D0` | `CRenderView::CollectLookingGlassInformation` | `40 53 48 83 EC 20 80 B9 C0 0F 00 00 00 48 8B D9 0F 84 41 01 00 00 65 48 8B 04 25 58 00 00 00` | Translated from EGS `0xEB7FB0` by unique byte match, 2026-08-15 | static-only | Gated on `cmp byte ptr [rcx+0xFC0], 0`. Signature stops at 31 bytes because the next instruction is `8B 15` RIP-relative and would never translate. Note the leading bare `40` REX, the fragile form. Its only caller is R-036.  **Promoted into the fail-closed gate 2026-08-22** as `view.collect_looking_glass`; verified unique and byte-correct at its RVA in the installed DLL by the build doctor.|
| R-035 | `PreyDll.dll` | `0xEE4B00` | `CRenderView::EnableLookingGlass(bool)`; sets `m_bLookingGlassEnabled` at `CRenderView+0xFC0` | `88 91 C0 0F 00 00 C3` followed by `CC` padding | Translated from EGS `0xEB85E0` by unique byte match, 2026-08-15 | static-only | A 7-byte setter: `mov [rcx+0xFC0], dl; ret`. Too small for Ghidra to define as a function, so `get_function_callers` fails on it. Its single xref is a vtable slot at `0x181DCAEB8`, which locates **`CRenderView`'s vtable**.  **Promoted into the fail-closed gate 2026-08-22** as `view.enable_looking_glass`; verified unique and byte-correct at its RVA in the installed DLL by the build doctor.|
| R-036 | `PreyDll.dll` | `0xEE63C0` | `CRenderView::Job_PostWrite` | `40 53 55 57 48 81 EC 80 00 00 00 48 8B E9 48 81 C1 18 18 00 00` | Reverse-translated from Steam bytes to EGS `0xEB9EA0`, then named from `RenderView.h` `FJob_PostWrite`, 2026-08-15 | static-only | The sole caller of R-034, and the job behind the `JobRenderViewPostWrite` string at `0x181DCAF7B` found in the original stereo reconnaissance. **Looking Glass information is gathered in a post-write job on the render view, not in a nested scene render**, which is the definitive reason R-028's `m_SceneRecurseCount` never moved. Signature stops at 21 bytes, before a `rel32` call.  **Promoted into the fail-closed gate 2026-08-22** as `view.job_post_write`; verified unique and byte-correct at its RVA in the installed DLL by the build doctor.|

| R-037 | `PreyDll.dll` | `0x1DD2E08` | `CD3D9Renderer` vtable | Not a signature; located by arithmetic from R-004's live-verified slot | Derived from R-004 plus byte search for the R-002/R-003 function pointers, 2026-08-15 | reproduced | Base found without RTTI, which is absent for the renderer. R-004 live-verified that the singleton's vtable calls slot `+0x8D0` for R-003, and the pointer to R-003 occurs exactly once in the image, at `0x181DD36D8`; `0x181DD36D8 - 0x8D0 = 0x181DD2E08`. Independently confirmed by R-002's pointer at `0x181DD36D0`, giving the same base via `- 0x8C8`. **This is the live renderer's vtable**, since R-004's dispatch was captured in a running process. Known slots: `+0x198` (index 51) R-032 `GetRenderViewForThread`, `+0x8C8` (281) R-002, `+0x8D0` (282) R-003. |
| R-038 | `PreyDll.dll` | `0x1DD7B18` | `CRenderer` vtable (abstract base) | Not a signature; located by shared slot index with R-037 | Byte search plus pure-virtual slot confirmation, 2026-08-15 | observed | The second vtable containing R-032. Taking the same slot index 51 gives base `0x181DD7CB0 - 0x198 = 0x181DD7B18`. At that base, slots `+0x8C8` and `+0x8D0` both hold `0x181B9AE62`, which disassembles as `FF 25 90 8E 0D 00` — `jmp qword ptr [rip+...]`, an IAT import thunk, i.e. `_purecall`. Two adjacent pure-virtual slots exactly where the concrete vtable has R-002 and R-003 identifies this as the abstract base. `GetRenderViewForThread` is a real implementation in both, which is why it appears in each. Confidence is `observed` rather than `reproduced`: the base rests on assuming the shared slot index, which is sound for a single-inheritance vtable prefix but was not independently measured. |

| R-039 | `PreyDll.dll` | `0x224DA60` | Pointer to the `ISystem` global; vtable `+0x388` is `ISystem::GetViewCamera()` returning `CCamera&` | Loaded and called inside R-011 | Decompilation of R-011 plus PDB header cross-check, 2026-08-22 | static-only | Every field R-011 reads through this pointer maps to a named `CCamera` member: `m_Matrix` at `+0x00`, `m_fov` at `+0x30` (passed to `tanf(x*0.5)`), `m_Width`/`m_Height` at `+0x38`/`+0x3C`, `m_ProjectionRatio` at `+0x40`, `GetNearPlane() = m_edge_nlt.y` at `+0x4C`, and `GetFarPlane() = m_edge_flt.y` at `+0x64`. `ISystem::SetViewCamera`/`GetViewCamera` are declared at `CrySystem/ISystem.h:1308-1309`. **This is the camera whose contamination H-008 was about** — see that entry before writing any camera. Never observed executing. **The slot index is now independently verified twice, 2026-08-22.** Counting only the virtuals declared directly in `struct ISystem` — excluding two that belong to the nested `ILoadingProgressListener` and four inside `#if 0` blocks — puts `GetViewCamera` at index 113, exactly `+0x388`. Separately, the `CSystem` vtable was located via `AutoDetectSpec` (R-043) and its `+0x388` slot reads `0x180DF2BB0`, whose body is `LEA RAX,[RCX+0x788]; RET`. See R-040 and R-041: the camera is the member `CSystem::m_ViewCamera` at `+0x788`, and its setter is a bare assignment. |
| R-040 | `PreyDll.dll` | `0xDF2BB0` | `CSystem::GetViewCamera()` returning `CCamera&` | `48 8D 81 88 07 00 00 C3` | `CSystem` vtable slot resolution plus PDB header slot count, 2026-08-22; bytes and uniqueness verified against the installed DLL | reproduced | The entire function is `LEA RAX,[RCX+0x788]; RET`. **The global view camera is the member `CSystem::m_ViewCamera` at `+0x788`, not a pointer** — so R-011 and R-016 both read a fixed offset from the `CSystem` object, and the camera can be observed without calling anything. The 8-byte signature is unique in the image and pins that offset, which is the fact [H-008](HYPOTHESES.md) rests on; **promoted into the fail-closed gate** as `system.get_view_camera`. Never observed executing. **Confirmed live 2026-08-29**: `CSystem+0x788` decoded as a plausible camera -- pos `331.08,756.65,481.70`, fov `1.5447413`, `2560x1440`, near `0.1`, far `8000`. See [live capture](../captures/traces/2026-08-29-prey-hurdle1-live-capture.md). **Byte-level proof added 2026-08-29.** `pSystem` vtable `+0x388` (RVA `0xDF2BB0`) disassembles to `48 8D 81 88 07 00 00 C3` = `lea rax,[rcx+0x788]; ret` -- `GetViewCamera` is literally `this + 0x788`, so the offset is confirmed from the accessor itself rather than only from its consumers. Read passively; nothing was executed in the game. |
| R-041 | `PreyDll.dll` | `0xDF4560` | `CSystem::SetViewCamera(CCamera&)` | `48 81 C1 88 07 00 00` | Same resolution as R-040; bytes and uniqueness verified against the installed DLL | static-only | The entire function is `ADD RCX,0x788; JMP CCamera::operator=` — **a pure member assignment that notifies nothing, invalidates no cache and never touches the renderer.** This materially sharpens H-008: the contamination risk comes entirely from *readers* observing a modified `m_ViewCamera`, not from any work the setter does. A scoped set/restore is therefore side-effect-free *at the setter*; whether it is safe overall still depends on who reads in between, which is what H-008 answers. Signature stops at 7 bytes because the following `JMP` carries a build-specific `rel32`. **Promoted into the fail-closed gate** as `system.set_view_camera`. Never observed executing. |
| R-042 | `PreyDll.dll` | `0x111C70` | `CCamera::operator=(const CCamera&)` | Tail-call target of R-041 | Ghidra decompilation, 2026-08-22; prologue confirmed on disk | static-only | A compiler-generated memberwise copy: ~0x8F dwords followed by two bitfield merges (`& 1` and `& 0xE`), no calls and no recomputation of frustum or matrices. Implies **`sizeof(CCamera) == 0x240`**. Copying a camera is pure data movement, so a saved/restored camera is bit-exact. |
| R-043 | `PreyDll.dll` | `0x1D9B9C8` | `CSystem` vtable | Slot contents, not a code signature | Located from `ISystem::AutoDetectSpec` at the header-derived slot, 2026-08-22; four slots verified on disk | reproduced | Found by taking the one function in this cluster whose name the PDB headers *do* expose, computing its slot from `ISystem.h`, and subtracting. Verified slots: `+0x018` `GetGlobalEnvironment` → `0x180903CA0`, `+0x380` `SetViewCamera` → `0x180DF4560`, `+0x388` `GetViewCamera` → `0x180DF2BB0`, `+0x480` `AutoDetectSpec` → `0x180D870C0`. Three of the four were predicted before being read. **Whole-table alignment then confirmed across all 193 slots**, see [`SYSTEM_VTABLE.md`](SYSTEM_VTABLE.md): slots named `Get`/`Is` land on a trivial accessor 74 times out of 99 while other slots do so 2 times out of 94 (and those two, `NeedDoWorkDuringOcclusionChecks` and `WasInDevMode`, are getters by any other name). Decisively, 24 accessors compile to `MOV RAX,[this+0x28]; MOV RAX,[RAX+d]; RET` — relaying through the `gEnv` pointer at `CSystem+0x28` — and **all 24 values of `d` hit the member the header gives that accessor's name**. This vtable is a verified general bridge from any named `ISystem` virtual to its Steam implementation address. **Confirmed live 2026-08-29** via the on-load snapshot: `system_vtable=yes`. See [live capture](../captures/traces/2026-08-29-prey-hurdle1-live-capture.md). |
| R-044 | `PreyDll.dll` | `0x224D980` | `gEnv`, the `SSystemGlobalEnvironment` instance | Base address, not a code signature | Three-point fit against the header layout, 2026-08-22 | reproduced | Derived, not guessed: three globals seen in disassembly land on three named members from one common base — `0x18224DA40` used as an `IConsole` (`GetCVar` at vtable `+0xB8`) is `pConsole` at `+0xC0`; `0x18224DA60`, the R-039 `ISystem` pointer, is `pSystem` at `+0xE0`; and `0x18224DAA0`, null-checked in R-046 to mean "the renderer is not up yet", is `pRenderer` at `+0x120`. All three offsets come from `ISystem.h` and all three fit simultaneously. **Independently confirmed 24 times over**: the `gEnv`-relaying `ISystem` accessors each index this struct, and every one lands on its own correctly-named member — including CryEngine's two aliases, `GetIAnimationSystem() -> pCharacterManager` and `GetIPak() -> pCryPak`. Reached at runtime from `CSystem+0x28`. Gives a named, verified route to 46 engine subsystem pointers; full layout in [`SYSTEM_VTABLE.md`](SYSTEM_VTABLE.md). **Confirmed live 2026-08-29**: resolved to `0x7FFD15D9D980`, and all three subsystem pointers followed from it (`pSystem`, `pRenderer`, `p3DEngine`). See [live capture](../captures/traces/2026-08-29-prey-hurdle1-live-capture.md). |
| R-045 | `PreyDll.dll` | `0xD870C0` | `ISystem::AutoDetectSpec(const bool detectResolution)` | 24-byte prologue is **not** unique — 3 hits | Log strings, PDB signature match, and vtable slot `+0x480`, 2026-08-22 | static-only | Identified by "Running machine spec auto detect (%d bit)..." and "- Final rating: Machine class %d", by the exact PDB signature at `ISystem.h:1409`, and independently by its vtable slot. Rates CPU/memory/GPU into a machine class, then defaults `r_Width`/`r_Height` from `GetSystemMetrics(SM_CXSCREEN/SM_CYSCREEN)` and fullscreen to 1 when `detectResolution` is set. **Not gated**: its prologue collides at `0xDA7395` and `0x1474F60`, another instance of the R-002 hazard, and it is not on any mod hook path. |
| R-046 | `PreyDll.dll` | `0xD87710` | DXGI adapter detection: `bool(DXGI_ADAPTER_DESC1* outDesc, uint* outFeatureLevelRank)` | `48 89 4C 24 08 56 41 55 41 56 48 81 EC F0 01 00 00 4C 8B EA 41 B8 38 01 00 00 33 D2 48 8B F1` (31B, unique) | Ghidra decompilation and IID/vtable-slot confirmation, 2026-08-22 | static-only | **This closes the R-001 open item.** The second `CreateDXGIFactory1` consumer is *not* a second render or present path — it is spec-detection telemetry. It creates a factory with `IID_IDXGIFactory1` (`{770AAE78-F26F-4DBA-A829-253C83D1B387}`, confirmed byte-for-byte at `0x1D933F8`), walks `EnumAdapters1` until `DXGI_ERROR_NOT_FOUND`, probes each adapter with `D3D11CreateDevice` over feature levels `{11_0, 10_1, 10_0, 9_3, 9_2, 9_1}`, requires a connected output via `EnumOutputs`, and keeps the highest-ranked adapter's `DXGI_ADAPTER_DESC1` (0x138 bytes, confirmed by the `memset` size, the `GetDesc1` slot and the copy loop). It creates and immediately releases its devices; nothing survives. It honours `r_overrideDXGIAdapter` the same way R-025 does, which is how that cvar was first noticed; the load-bearing use is R-025's, since that is the path that creates the device the OpenXR runtime will see. |
| R-047 | `PreyDll.dll` | `0xD87C80` | Cached GPU-info query: description, vendor, device, VRAM, feature-level rank | `48 89 6C 24 18 48 89 74 24 20 57 41 56 41 57 48` … (24B, unique) | Ghidra decompilation, 2026-08-22 | static-only | Runs R-046 once behind a `did-run` flag at `0x2261C60` and caches into `0x2261C68` (description string), `0x2261E80`/`0x2261E84` (VendorId/DeviceId), `0x2261E88` (dedicated video memory) and `0x2261E90` (feature-level rank). Read-only and safe to call; a cheap way to learn which adapter Prey considers best without touching DXGI. Not gated — diagnostic only, on no hook path. |
| R-048 | `PreyDll.dll` | `CSystem+0x788` | `CCamera` layout, `sizeof == 0x240`; the live instance is `CSystem::m_ViewCamera` | Struct layout, not a code signature | `Cry_Camera.h` cross-checked against 11 offsets observed in the Steam binary, 2026-08-22 | static-only | Full table in [`CCAMERA_LAYOUT.md`](CCAMERA_LAYOUT.md). The header is for the EGS build, so agreement was measured: six head offsets from R-039 (`m_fov` `+0x30` … `m_edge_flt` `+0x64`) and five tail offsets from R-042's copy widths (`m_pPortal` `+0x218` and `m_pMultiCamera` `+0x228` copied as qwords, `m_ScissorInfo` `+0x220`, `m_OccPosition` `+0x230`, and the `+0x23C` bitfield merged with masks `& 1` and `& 0xE` — exactly `m_JustActivated : 1; m_sceneMaskFilter : 3`) all land on member boundaries, and the computed size matches R-042's measured `0x240`. **VR-critical:** `m_asymL/R/B/T` at `+0x6C`/`+0x70`/`+0x74`/`+0x78` make an OpenXR per-eye asymmetric projection directly representable. **These four offsets are no longer header-derived: R-030 reads all four** (as `param[0x1B]`..`param[0x1E]`) and folds them into the render view's frustum tangents, which confirms both the offsets and that the path is live. The header still annotates them *"not used for culling atm"*, so expect them to change what is rendered without changing what is culled. `m_fp`, the `m_id*` arrays and the twelve cached corner vertices are derived data held inside the struct: writing `m_Matrix` or `m_fov` directly does not refresh them. **Negative result worth stating, 2026-08-22:** `CCamera` contains **no AnvilNext-style view-matrix override pointer**. Because the layout closes byte-exactly at `0x240` with no unexplained slack, this is a definitive absence rather than a failure to find one — the only two pointers are `m_pPortal` (`IVisArea*`) and `m_pMultiCamera`, both accounted for. **But `m_pMultiCamera` at `+0x228` is a lead**: the header comments it *"Maybe used for culling instead of this camera"*, i.e. a nullable pointer that substitutes a **different camera for culling**. That is the shape of a native answer to this entry's own culling caveat — a combined symmetric frustum covering both eyes could be supplied for culling while each eye renders asymmetrically. Consumer untraced; recorded as a lead, not a finding. |
| R-049 | `PreyDll.dll` | `CRenderView+0x11A0` | `CRenderView::m_camera`, a full `CCamera` by value | Destination of `ADD RCX,0x11A0` at function offset `+0x25` of R-030 | Disassembly of R-030 verified on disk, 2026-08-22 | static-only | R-030 begins `MOV RDI,RCX; ADD RCX,0x11A0; MOV RBX,RDX` and tail-calls `CCamera::operator=` (R-042) with `RDX` untouched, so the render view **copies** the camera rather than referencing it. Spans `+0x11A0`..`+0x13E0` at `sizeof(CCamera)==0x240` (R-048). **Safety property for [H-008](HYPOTHESES.md):** a camera written through R-030 lands in the render view's own copy and cannot alias `CSystem::m_ViewCamera`. |
| R-050 | `PreyDll.dll` | `CRenderView+0x1620` | `CRenderCamera` — the derived view/frustum block the renderer actually consumes | Six stores at disp32 `0x1620`/`0x1630`/`0x1640`/`0x1650`/`0x1660`, function offsets `+0x2DE`..`+0x309` | Decompilation of R-030 plus on-disk disp32 confirmation, 2026-08-22 | static-only | Layout from `IRenderer.h:545-548` (`class CRenderCamera`), non-virtual so it starts at its first member: `vX` `+0x1620`, `vY` `+0x162C`, `vZ` `+0x1638`, `vOrigin` `+0x1644`, then **`fWL` `+0x1650`, `fWR` `+0x1654`, `fWB` `+0x1658`, `fWT` `+0x165C`**, `fNear` `+0x1660`, `fFar` `+0x1664`. **CORRECTED 2026-08-29 by live capture.** The four `fW*` are **glFrustum near-plane coordinates**, i.e. `tangent * near`, **not raw tangents** -- matching `CRenderCamera::Frustum(l, r, b, t, Ndist, Fdist)`. The documented formula omitted the near-plane factor and scored a residual of **1.5588** against a real frame; with `t' = tan(fov/2) * near` it scores **1e-9**. The error was a misreading of our own decompilation: the line `fVar8 = fVar8 * fVar1` (with `fVar1` = near) was present in the quoted output and was dropped when the formula was written down. OpenXR's `XrFovf` still maps on, but each tangent must be scaled by the near plane first. This also establishes the **live rounding floor at ~1e-9**. See [live capture](../captures/traces/2026-08-29-prey-hurdle1-live-capture.md). |
| R-051 | `PreyDll.dll` | `0xEE68D0` | `CRenderCamera::LookAt(const Vec3& Eye, const Vec3& ViewRefPt, const Vec3& ViewUp)` | Called from R-030 with four arguments | Argument shape matched to `IRenderer.h:504`, 2026-08-22 | static-only | R-030 builds an orthonormal basis from the camera matrix, forms `Eye` = camera position, `ViewRefPt` = position + forward, and `ViewUp` from the cross products, then calls this to fill `vX/vY/vZ/vOrigin`. Not gated — reachable only through R-030, which is gated. |
| R-052 | `PreyDll.dll` | `0xF50A00` | `GetOverrideDXGIAdapter()` -> `int`; reads the `r_overrideDXGIAdapter` cvar | Sole caller is R-025 at `0xF5008D` | Decompilation plus disassembly of the call site, 2026-08-22 | static-only | Returns the cvar's int value via `gEnv->pConsole->GetCVar("r_overrideDXGIAdapter")->GetIVal()`, or `-1` when the console or the cvar is missing. The cvar name string is at `0x1D93318`. **Its significance is the call site**, see R-025. |
| R-053 | `PreyDll.dll` | `0x1DCAE00` | `CRenderView` vtable, 24 slots (`IRenderView : CMultiThreadRefCount`) | Slot contents, not a code signature | Located from R-030's `.rdata` reference at the header-derived slot, 2026-08-22 | reproduced | R-030 has **no direct call xrefs** — only two DATA references, one in `.pdata` (its unwind entry) and one in `.rdata` at `0x1DCAE40`. `SetCamera` is therefore **virtual**, dispatched through this vtable. `IRenderer.h:585` puts it at index 8 counting the two inherited `CMultiThreadRefCount` virtuals, giving base `0x1DCAE00`. **Independently confirmed**: slot 23 predicts `EnableLookingGlass` and reads `0xEE4B00`, which is R-035 — a hit at the far end of the table from the anchor. Practical consequence: the seam can be taken by swapping a vtable slot rather than patching function bytes. Note the render views are pooled (R-026), so the vtable is shared across instances and either technique affects all of them. **Confirmed live 2026-08-29**: all four pooled `CRenderView` objects carry this vtable at `0x7FFD1591AE00` (= base `0x7FFD13B50000` + `0x1DCAE00`). See [live capture](../captures/traces/2026-08-29-prey-hurdle1-live-capture.md). |
| R-054 | `PreyDll.dll` | `0x21F520` | `C3DEngine::RenderWorld(int nRenderFlags, const SRenderingPassInfo&, const char* szDebugName)`; `C3DEngine` vtable at `0x1C912A0` | Vtable slot 3 (`+0x18`), inherited from `IProcess` | Identified by its own `"RenderWorld"` profile marker and 4-parameter shape, then confirmed by vtable slot, 2026-08-22 | reproduced | **This is the engine's world-render entry, and it is reachable as a vtable slot.** It has **zero direct call xrefs** — four DATA references only, one `.pdata` unwind entry, two non-8-aligned `.rdata` hits, and the vtable slot — so every call is virtual dispatch through `gEnv->p3DEngine` (`gEnv+0x08`) `-> [vtable+0x18]`. `RenderWorld` is declared on `IProcess` (`IProcess.h`), the base of `I3DEngine`, at index 3. Vtable alignment confirmed by shape: slot 5 `SetFlags` is `MOV [RCX+8],EDX; RET` and slot 6 `GetFlags` is `MOV EAX,[RCX+8]; RET` — a setter and getter on the *same* member, predicted by name and matching independently. **The camera enters through the `SRenderingPassInfo` argument** (R-055), so a per-eye world render needs no matrix patching. Never observed executing; nothing has been called. **Confirmed live 2026-08-29**: `IProcess` slot 3 resolved to `0x21F520`, and the function was observed executing 33/s on thread 44208 from a **single** caller `PreyDll+0xE0BC62`, with `szDebugName="CSystem::Render"` on 66/66 calls and `nRenderFlags=0xf`. See [live capture](../captures/traces/2026-08-29-prey-hurdle1-live-capture.md). |
| R-055 | `PreyDll.dll` | `0x1E5B30` | `SRenderingPassInfo::CreateGeneralPassRenderingInfo(const CCamera&, uint32 nRenderingFlags, bool bAuxWindow)` | `48 89 5C 24 08 48 89 6C 24 10 56 57 41 56 48 83 EC 30 45 33 F6 C6 41 01 00 4C 89 71 04 48 8B F1 44 89 71 14 BD 04 00 00 00` (41B, unique) | Translated from the PDB header RVA `0x1E5E60` by unique byte signature, 2026-08-22 | static-only | The header exposes this one as a real `PreyFunction` rather than inline (`I3DEngine.h:2684`), unlike `CreateRecursivePassRenderingInfo`. **It takes an arbitrary `CCamera&`**, which is what makes a per-eye pass constructible. **Exactly 3 direct callers**, a small enough set to use as a validator later. One of them, `0x20D960`, shows the canonical pattern: it calls `gEnv->pSystem` vtable `+0x388` (`GetViewCamera`), hands the result straight to this function with flags `0x2E5DF` and `bAuxWindow = 0`, and passes the result on to a render call. `sizeof(SRenderingPassInfo)` is about `0x50` from the caller's stack slot. EGS-to-Steam delta here is `-0x330`, a sixth distinct delta. |
| R-056 | `PreyDll.dll` | `gameCVars+0x284` | `g_detachCamera`, backing int of the detached-camera cvar family | Registration `LEA R8,[RDI+0x284]` at `0x171C1EC`; name string at `0x1E805A8` | Disassembly of the registration and of the `UnregisterVariable` cleanup sweep, 2026-08-22 | static-only | Prey registers a full detached-camera family — `g_detachCamera`, `g_moveDetachedCamera`, `g_detachedCameraMoveSpeed`, `g_detachedCameraRotateSpeed`, `g_detachedCameraTurboBoost`, `g_detachedCameraDebug` — plus `e_CameraFreeze` and `e_CameraGoto`. Registration is confirmed twice: the `Register` call binds the backing int at `+0x284`, and a later cleanup pass unregisters the name via `IConsole::UnregisterVariable(name, true)`. **Traced 2026-08-22; the result is an honest inconclusive that leans dead.** Established: the family is six contiguous ints/floats at `+0x284`..`+0x298`; the registration function *is* reached, by a tail `JMP` at `0x1727AA3` from a wrapper at `0x1727A90` doing `MOV RAX,[RCX+0xF8]; TEST RAX,RAX; JZ; MOV RCX,RAX` -- so the cvars struct is **heap-allocated at `CGame+0xF8`**, not a global, and the cvars really are registered. Nothing looks any of them up by name: the only two string xrefs are the registration and the `UnregisterVariable` cleanup. **No consumer was found, but that negative is not trustworthy** -- the same search also failed to find consumers for control cvars in the same struct that certainly are consumed, notably `g_difficultyLevel` at `+0x29C`; see [F-007](FAILURE_REGISTRY.md). **Strong circumstantial evidence of vestigial Crysis GameSDK code:** the registered help strings are unmodified GameSDK boilerplate -- *Move speed turbo boost when holding down (360) A button* and *Display debug graphics for detached camera spline playback* -- and the same block registers `g_mpNoVTOL`, `g_mpHatsBootsOnRadar`, `g_maxGameBrowserResults` and `g_randomSpawnPointCacheTime`, for systems Prey does not have. **Earlier suggestion withdrawn:** do not treat this as an available zero-hook live experiment. Settling it costs one console command in a running game, `g_detachCamera 1`, which is the cheapest next step rather than more static analysis. Either way it is a *whole-view* override, never a per-eye mechanism. |
| R-057 | `PreyDll.dll` | `0x210E10` | `C3DEngine::UpdateRenderingCamera(const char* szCallerName, const SRenderingPassInfo&)`; the **only** caller of `CRenderView::SetCamera` (R-030) | Call site at `+0x2110E5` is `call [rax+0x40]` on the view fetched by `mov rbx,[r14+0x20]` | Frida `Interceptor` return-address census 2026-08-29 (432/432 calls from this one site); Ghidra `get_function_by_address` + decompilation confirmed the containing function and `get_function_callers` confirmed its sole caller is `FUN_18021f520` = `RenderWorld` (R-054) | reproduced | Chain is `CSystem::Render (R-058) -> RenderWorld (R-054) -> UpdateRenderingCamera -> CRenderView::SetCamera`. **Exactly one caller**, so H-009's deny-by-default return-RVA gate is trivially implementable. Contains two camera paths — see R-063. Immediately after `SetCamera` it calls `SetPreviousFrameCamera` (R-064) with a *different* camera; any per-eye write must handle both. |
| R-058 | `PreyDll.dll` | `0xE0BA30` | `CSystem::Render`; the **only** caller of `C3DEngine::RenderWorld` (R-054) | `lea r9,[rip+0xf99fc5]` -> `"CSystem::Render"`, `mov edx,0xf`, `call [rbx+0x18]` at `+0xE0BC62` | Frida return-address census 2026-08-29; Ghidra decompilation 2026-08-29 | reproduced | Independently re-confirms four offsets from a second site: `this+0x788` = `m_ViewCamera` (R-040), `this+0x28` = `gEnv` (R-039), `gEnv+0x08` = `p3DEngine`, and `gEnv+0x120` = `pRenderer` (R-005/R-044) which Ghidra names `DAT_18224daa0`. Builds the pass info with `CreateGeneralPassRenderingInfo(&m_ViewCamera, 0x2E5DF, 0)` — so **`m_ViewCamera` is the single source feeding both the 3DEngine camera update and the pass**. Gated on a bool at `CSystem+0x9D7`. |
| R-059 | `PreyDll.dll` | `CSystem+0xAB0` | `CSystem::m_pProcess` (`IProcess*`), the object whose vtable slot 3 is `RenderWorld` | `param_1[0x156]` in R-058; null-checked, then `(**(code**)(*it+0x18))(it, 0xf, passInfo, "CSystem::Render")` | Ghidra decompilation of R-058, 2026-08-29 | reproduced | Completes the R-054 dispatch story: `RenderWorld` is reached through `CSystem::m_pProcess`, not through `gEnv->p3DEngine` directly, even though the object is the same `C3DEngine`. Not yet read live. **Live 2026-08-29:** `CSystem+0xAB0` read as `0x1AF9F0A9200`, which is *exactly* `gEnv->p3DEngine` (delta `0x0`), so `IProcess` is `C3DEngine`'s primary base and needs no pointer adjustment. Its vtable RVA is `0x1C912A0` -- matching `kThreeDEngineVtableRva` -- and slot 3 resolves to `0x21F520`, which is R-054. |
| R-060 | `PreyDll.dll` | `C3DEngine+0x610` | `C3DEngine::m_RenderingCamera`, the engine's current culling/rendering camera (`CCamera`) | `param_1 + 0xc2` in R-057; saved to a local then overwritten each frame | Ghidra decompilation of R-057, 2026-08-29 | reproduced | The saved copy becomes the argument to `SetPreviousFrameCamera`. **Not written at all on the freeze path** (R-063), which is what decouples culling from rendering. `C3DEngine` vtable `+0x78` returns this camera — its `Matrix34` translation is read at `+0xc/+0x1c/+0x2c`, matching the documented `m[3]/m[7]/m[11]` layout. **Live 2026-08-29:** decoded as a `CCamera` and compared byte-for-byte against `CSystem::m_ViewCamera` (R-040). The two `0x240` blocks differ in **8 bytes of 576**, at `+0x214` and `+0x23C` only -- both inside the cached-derived-state region, consistent with a bookkeeping or frame tag. Every field the VR path touches is identical: position `325.847, 740.602, 482.790`, fov `1.5447413`, `2560x1440`, ratio `1.7777778`, near `0.1`, far `8000`, and asymmetry `0,0,0,0` in **both** copies. |
| R-061 | `PreyDll.dll` | n/a (struct) | `SRenderingPassInfo` field layout | `+0x00` slot id, `+0x04` flags, `+0x08` zoom, `+0x0C` frame id, `+0x10` prev frame id, `+0x18` `CCamera*`, `+0x20` `CRenderView*`, `+0x48` sentinel | Live Frida dump of `RenderWorld`'s third argument 2026-08-29 (`+0x04 = 0x2E5DF`, `+0x0C` incrementing by 1, `+0x20` == the two Default views); `+0x18`/`+0x20` independently confirmed by Ghidra decompilation of R-057 | reproduced | `+0x20` is the pass-to-view link a second per-eye pass must populate; `+0x18` is the camera `UpdateRenderingCamera` copies from. `+0x04`'s `0x2E5DF` is the same constant `CreateGeneralPassRenderingInfo` is called with in R-058. `+0x08` (observed `1.0f`) scales streaming priority. |
| R-062 | `PreyDll.dll` | n/a (struct) | `CRenderView` header fields: `+0x10` `EUsageMode`, `+0x14` `EViewType`, `+0x20` pass flags, `+0x24` `nRenderFlags` | Diff of a Default view against a Recursive one, cross-checked against a `SwitchUsageMode` trace | Live Frida 2026-08-29: `+0x10` tracked `SwitchUsageMode`'s argument exactly across all five modes; `+0x14` partitioned a 16-view census into 2 Default / 14 Shadow / 2 Recursive with matching resolutions and flags | reproduced | **Corrects an implicit assumption**: R-033's `m_pRenderViews[2][2]` pool holds only 4 of the 16 views scheduled per frame. The 14 Shadow views (640x480, flags `0xAE5DE`, renderFlags `0x0`) live outside the pool, so H-009 must account for views the pool does not enumerate. The 2 Recursive entries are allocated and typed but never scheduled and never given a camera — `usage` stays `Undefined`. |
| R-063 | `PreyDll.dll` | `0x243A688` | Pointer to the `C3DEngine` CVars struct; `+0x1CC` = `e_CameraFreeze`, `+0xD4` = `e_CoverageBufferDebugFreeze`, `+0x230` = `e_CameraRotationSpeed` (float), `+0x3E0` = `e_StreamCgfDebug` | `DAT_18243a688` dereferenced in R-057; name strings at `0x1C9B498`, `0x1C96FC8`, `0x1C9B3F8` | Live Frida 2026-08-29: struct pointer read as `0x1AF7C3A7F90`; each name resolved through `pConsole->GetCVar` and its backing pointer at `ICVar+0x48` matched the predicted field address exactly. `e_CameraGoto` and `e_Recursion` were run as negative controls and correctly matched nothing | reproduced | **An engine-honoured camera override (META-006).** R-057's predicate is `if (!e_CameraFreeze && !e_CoverageBufferDebugFreeze)`. On the else-branch the engine skips writing R-060 and feeds `SetCamera` from `gEnv->pSystem->GetViewCamera()` (`pSystem` vtable `+0x388`) instead of the pass camera, using a static cached camera at `0x243A820` as the previous frame. **Not a clean seam**: freezing the culling camera is a side effect, not an option. Recorded because it proves `m_ViewCamera` can reach `SetCamera` directly on a path the engine already ships. |
| R-064 | `PreyDll.dll` | `0xEE82D0`, `0xEE82F0` | `CRenderView::SetPreviousFrameCamera` (vtable slot 9, `+0x48`) and `CRenderView::SwitchUsageMode` (slot 10, `+0x50`) | Resolved from the live vtable at `0x7FFD1591AE00` (R-053) | Live Frida 2026-08-29: all 24 slots matched the header-derived ordering; `SwitchUsageMode` traced for 1 s giving 72 of each of the five modes | reproduced | `SetPreviousFrameCamera` is called immediately after every `SetCamera` with a different camera. With `r_MotionBlur = 2` and `r_AntialiasingMode = 3` both live, it is load-bearing for motion vectors and temporal reprojection — **a per-eye write that ignores it will give the second eye the wrong reprojection history.** `SwitchUsageMode` shows `Writing -> WritingDone` on the game thread (44208) and `Reading -> ReadingDone` on the render thread (54600). |
| R-065 | `PreyDll.dll` | `0x3D91C0` | The **only** caller of `ArkPlayerCamera::UpdateView` (R-009) | Call site at `+0x3D94B0` | Frida return-address census 2026-08-29, 41/41 calls from this one site | reproduced | Makes R-009 a single-caller seam like R-057 and R-058. `UpdateView` converts a world pose (`325.83, 740.60, 482.79`) on entry into a local eye offset (`-0.031, -0.003, 1.6999`, i.e. 1.7 m eye height) on exit, preserving the quaternion and fov — the shape a VR eye offset composes with. The custom-view callback at `ArkPlayerCamera+0x148` was `NULL` throughout. |
| R-066 | `PreyDll.dll` | `0x243A658`, `0x243A820` | `C3DEngine`'s cached `IRenderer*` (`SetCamera` at vtable `+0x188`), and the static frozen previous-frame `CCamera` used on the R-063 freeze path | `DAT_18243a658` and `DAT_18243a820` in R-057 | Ghidra decompilation of R-057, 2026-08-29 | reproduced | `0x243A820` is initialised once, thread-safely (`_Init_thread_header`/`_Init_thread_footer` on the guard at `0x243AA60`), from `gEnv->pSystem->GetViewCamera()`. Not yet read live. **Live 2026-08-29:** `0x243A658` read as `0x7FFD16674E80`, equal to `gEnv->pRenderer`, and its vtable `+0x188` resolves to RVA `0xF7FE70` (`IRenderer::SetCamera`). The frozen camera at `0x243A820` is **all zeros** and the init-once guard at `0x243AA60` is `0`, confirming the freeze branch has never executed this session -- which is the positive control for the control-flow reading in R-063, since that static is initialised only inside that branch. |
| R-067 | `PreyDll.dll` | `0x121D70` | `CCamera::UpdateFrustum()`; rebuilds every cached field a matrix or FOV write invalidates | Prologue `48 8B C4 55 53 48 8D 68 A1 48 81 EC F8 00 00 00 F3 0F 10 59` | Found by decompiling the function `C3DEngine::UpdateRenderingCamera` (R-057) calls immediately after rotating the matrix for `e_CameraRotationSpeed`; the body writes exactly the offsets `CameraLayout` documents as derived state | static-only | **The missing piece for writing a camera correctly.** Rebuilds the eight frustum corners (`+0x7C..+0x108`), the six planes at **`+0x10C`** — matching `CameraLayout::frustumPlanes` — the plane sign/index tables (`+0x16C..+0x1F8`), and the cached position at `+0x230`. Writing a matrix without calling it leaves all of that describing the previous camera. Takes `CCamera*` and touches nothing else, so it is safe to call on a private copy; `CameraEditHook` does exactly that and only then blits. |
| R-068 | `PreyDll.dll` | `0x11A310` | The orthonormality predicate `CCamera::UpdateFrustum` uses to decide whether to negate its plane normals | Nine cross-product comparisons against `(float* matrix, float epsilon)` | Decompiled 2026-08-30 while building the bounded camera write | static-only | **If this returns false, `UpdateFrustum` negates all six frustum plane normals** — so a denormalising matrix write does not render slightly wrong, it inverts culling. Transcribed verbatim into `preyvr::cameraedit::RotationIsOrthonormal`; paraphrasing it as "each column is the cross product of the other two" flipped a sign on the first attempt. **It also has a hole:** an all-zero matrix satisfies all nine comparisons, since each reduces to `|0 - 0|`. `RotationIsDegenerate` closes it and writes are gated on both. |
| R-069 | `PreyDll.dll` | `0x2B1C64C`, `CD3D9Renderer+0x95B4` | `r_DrawNearFoV` backing float, and the per-frame copy the renderer actually uses | Registration `LEA R8,[0x182B1C64C]` beside the name string at `0x1CA9470`; latched by `MOVSS [RBX+0x95B4],XMM0` in `CD3D9Renderer::RT_BeginFrame` | Static xref analysis 2026-08-30; live value `54` read through the console on 2026-08-29 | static-only | **Answers the H-005 sub-question: the viewmodel FOV is latched once per frame, not read per draw.** `RT_BeginFrame` is the only reader of the cvar besides the `EF_Query` get/set path at `FUN_180FE1300`. So writing the *cvar* between two eye renders inside one frame cannot give per-eye viewmodel FOV — the latch has already happened. **`CD3D9Renderer+0x95B4` is the actual per-eye lever.** Relevant because the viewmodel renders at `54` degrees against the world's `88`, so it does not share the world's projection and will not follow a per-eye camera on its own. |
| R-070 | `PreyDll.dll` | n/a (subsystem) | CryEngine's stock action-map input stack is present and unmodified | `CActionMap::*` diagnostic strings at `0x1CBE608..0x1CBEC60`; `Input:ActionMaps:ActionMapManager`, `:ActionMap`, `:ActionFilter`, `:ActionListener` at `0x1CA3120..0x1CA32F0`; `CMouse::Init CreateDirectInputDevice` at `0x1D5CD60`; `i_xinput`, `i_xinput_poll_time`, `i_xinput_deadzone_handling`, `i_forcefeedback`, `i_mouse_*` cvars at `0x1D5E0F8..0x1D5E670` | String and cvar survey 2026-08-30 | static-only | **The route for motion controllers to drive native gameplay rather than a parallel simulation (H-006).** Prey did not replace CryEngine's input layer: actions are named and dispatched through `CActionMapManager`, and named actions including `attack1`, `firemode`, `reload` and `Crouch` are present in the binary. The standard injection seam in this engine is `IInput::PostInputEvent`, which feeds the same pipeline the keyboard and gamepad do, so a synthesised action reaches every consumer that a real button does. **Not yet located:** `gEnv`'s `pInput` slot and the `PostInputEvent` vtable index — both want a live process to confirm rather than a guess. |

| R-071 | `PreyDll.dll` | `0x1E5B30` | `SRenderingPassInfo::CreateGeneralPassRenderingInfo(SRenderingPassInfo* out, const CCamera*, uint32 nRenderingFlags, bool bAuxWindow)` | Called from R-058 as `FUN_1801e5b30(local_58, &m_ViewCamera, 0x2e5df, 0)`, its return value passed straight to `RenderWorld` (R-054) | Ghidra decompilation of R-058 and of this function, 2026-09-01 | static-only | **This is the seam for native stereo, and it removes the need to re-enter `CSystem::Render`.** The pass info is built into a caller-supplied **64-byte stack buffer** (`undefined1 local_58[64]` in R-058), so it is a value type constructed per call rather than a shared singleton -- constructing a second one per eye is what the design already supports. It writes every field R-061 documents: `+0x04` = the flags argument, `+0x08` = a zoom factor computed as `(cam+0x30 / cam+0x34) * 0.8 + 0.2`, `+0x0C`/`+0x10` = frame ids from `pRenderer->vtable[0x548](1)` and `(0)`, `+0x18` = the camera, and `+0x20` = the render view (see R-072). Contrast with F-013: A3 failed because re-entering `CSystem::Render` reuses one pass info and one render view for both eyes, so the second pass culls against state the first already consumed. |
| R-072 | `PreyDll.dll` | `pRenderer` vtable `+0x198` | `IRenderer::GetRenderViewForThread(int nThreadId, IRenderView::EViewType type)` -- the allocator of the `CRenderView*` at `SRenderingPassInfo+0x20` | `plVar5 = (**(code **)(*DAT_18224daa0 + 0x198))(DAT_18224daa0, *param_1, 0); *(longlong **)(param_1 + 0x20) = plVar5;` inside R-071, where `DAT_18224daa0` is `gEnv->pRenderer` (R-005/R-044) and `*param_1` is the slot id fetched from `pRenderer->vtable[0x888](6, ...)` | Ghidra decompilation of R-071, 2026-09-01; confirmed live in-game 2026-09-01 on both thread slots | reproduced | **The trailing `0` is a view-type selector, and that is the whole question for native stereo.** CryEngine's `IRenderView::EViewType` is `eViewType_Default = 0`, `eViewType_Recursive = 1`, `eViewType_Shadow = 2`, `eViewType_BillboardGen = 3`. Every pass Prey builds through R-071 asks for type **0**, which is consistent with R-061's live observation that `+0x20` was only ever one of *two* Default views -- the MT/RT double buffer -- and with R-063's note that `e_Recursion` matched no CVar field while the recursive views are "allocated and idle". **If type 1 returns a distinct view**, a second per-eye pass can be given its own render view and `RenderWorld` called twice without the re-entry that wedged the engine in F-013. **CONFIRMED LIVE 2026-09-01.** Type 1 returns a view distinct from type 0, on both thread slots:

```
slot=0 defaultView=0x2b3fb40fde0 recursiveView=0x2b3fb467b30 verdict=distinct
slot=1 defaultView=0x2b3fb4ad0b0 recursiveView=0x2b3fb4b1460 verdict=distinct
```

So each MT/RT slot owns its own Default *and* its own Recursive view -- four views, which is exactly `RendererLayout::renderViewPoolCount = 4` in the engine map, a number recorded before this was understood and now explained as 2 slots x 2 types. **This unblocks native stereo.** A second per-eye pass can be given its own render view, so `RenderWorld` can be called twice without the re-entry that wedged the engine in F-013. The enum ordering is still CryEngine's rather than read from these bytes, but the behaviour it predicts is now observed. |

| R-073 | `PreyDll.dll` | `SRenderingPassInfo+0x01` | The engine's own **secondary-pass flag**: when non-zero, `RenderWorld` (R-054) and its dispatch skip essentially all once-per-frame work and perform only the world render | `CreateGeneralPassRenderingInfo` (R-071) zeroes it as its first statement (`mov byte [rcx+1],0`, part of the `pass.create_general` landmark bytes); `RenderWorld` and `FUN_18021E170` then test `*(char*)(passInfo+1) == 0` at roughly nine sites | Ghidra decompilation of R-054 (`0x21F520`) and its dispatch (`0x21E170`), 2026-09-01 | static-only | **This is the engine's own answer to BN-SFX-001, and it is better than any gate we could add, because the skipping is the engine's rather than ours.** Guarded on the flag being zero: the frame counter at `C3DEngine+0xC6C`; `UpdateRenderingCamera` (R-057) -- so a secondary pass does **not** write the global rendering camera (R-060) or call `SetCamera`; the default-material setup; the CVar snapshot into `C3DEngine+0x2F/+0x17C/+0x30/+0x184`; an occlusion/bounding-box update and `FUN_1801FF450`; `FUN_1802114D0`; and a flag clear at `+0xBD1`. The **only** work outside those guards is `C3DEngine->vtable[0x688](nRenderFlags, passInfo)`, the world render dispatch itself. So a second pass marked secondary renders the world from the camera at `+0x18` into the view at `+0x20` while advancing none of the per-frame state whose reuse wedged the engine in F-013. Corroborates the reading of `e_Recursion` in R-063 as dormant-but-present recursion machinery. **`static-only`:** the control flow is read, not executed. A4 now sets this byte, and the A5 zero-delta control is what turns it into `reproduced`. |

Confidence values: `observed`, `reproduced`, `static-only`, `hypothesis`, or `invalidated`.
